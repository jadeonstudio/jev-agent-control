# Documentation only. This project NEVER automatically loads a repository .env.
# Set TYPESAFE_API_KEY in the process environment, or run jev-control key set
# yourself in a terminal. Never put a real key in this file or in agent chat.
# TYPESAFE_API_KEY=<set-outside-this-repository>
# JEV_DISABLE=1  # emergency override: blocks new API requests
